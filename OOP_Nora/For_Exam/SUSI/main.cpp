#include "libs.hpp"

#include "course.hpp"
#include "user.hpp"

int main() {

    /* UC - user control

    - check that size <= 200
    - if we enter sutdent and its mark is not in [2,..,6] just i++, so if the student hasn't mark we do nothing to marks[]
        but if mark is in [2,..,6] then write it in marks[]
    -

    */

    // Course c;
    //
    // cout << c << endl;
    //
    // Student sts[3];
    // double arr[3] = {6, 6, 6};
    // Course c1("Kalin", "Astea", "2018/2019", sts, arr, 3);
    // cout << c1 << endl;
    //
    // Course c2(c1), c3;
    // c3 = c1;
    //
    // cout << c2 << endl << c3 << endl;

    return 0;
}
